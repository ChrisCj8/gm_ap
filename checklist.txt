todo:
- set everything up for localization
- message type filtering for chat reader
- better way to update tracker tool setup info so it isn't sent every time an option is changed
- item/location group support for tracker
- gmod player to slot association (for deathlink and chat forwarding for example)
- deathlink support
- energylink support
- gifting support
- bounce packet reader?
- expression 2 support
- tool menu presets for tracker
- test if other tags are able to send locations
- chat reader settings may not be getting saved properly
- rework tracker hook system


- packet handling:
  - RoomInfo:
    gets saved to the slot
  - ConnectionRefused:
    could be handled better
  - Connected:
    gets saved to the slot
  - ReceivedItems:
    should be handled properly
  - LocationInfo:
    not handled yet since the client doesn't request location scouts yet
  - RoomUpdate:
    thought this was properly handled but apparently this can also contain arguments from roominfo and connected which still needs to be handled, currently only handles the alias renames and tag updates
    i'm not currently doing anything with any of the data this can update so i haven't fully addressed this yet
  - PrintJSON:
    gets displayed properly but some arguments are currently not processed
    most importantly, player_name is not processed in both the regular packet processor and the chat reader, which could cause problems in the future if team support is added
  - DataPackage:
    gets saved to the slot and cached
  - Bounced:
    logged but the game doesn't do much with them
  - InvalidPacket:
    displays the error text the server sent which should be enough?
  - Retrieved:
    gets saved to the room
  - SetReply:
    gets saved to the room